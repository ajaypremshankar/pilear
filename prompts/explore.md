---
description: Guided codebase walkthrough
argument-hint: "<path>"
---

Load and follow the `code-explore` skill (`/skill:code-explore`).

Path: $@

Write artifacts under `<learning-root>/<domain>/<subject>/`. Use the active learning root from session context.
