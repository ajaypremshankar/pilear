---
description: Stress-test a design doc or RFC the user will paste
---

Load and follow the `design-review` skill (`/skill:design-review`).

The user will paste a design, RFC, or architecture description in this message or the next.

Write artifacts under `<learning-root>/<domain>/<subject>/`. Use the active learning root from session context.
