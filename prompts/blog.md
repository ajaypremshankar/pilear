---
description: Turn prior learning into a publishable blog draft
argument-hint: "<topic>"
---

Load and follow the `blog` skill (`/skill:blog`).

Topic: $@

Read artifacts from `<learning-root>/<domain>/<subject>/`. Write `blog-draft.md` there only. Use the active learning root from session context.
