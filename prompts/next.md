---
description: Suggest next topics from your knowledge graph
argument-hint: ""
---

Load and follow the `discover` skill (`/skill:discover`) in **/next** mode.

Use **Graph /next candidates** from session context. If missing, ask user to run `/next` first.
