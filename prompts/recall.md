---
description: Retrieval practice on a topic you studied before
argument-hint: "<topic or domain/subject>"
---

Load and follow the `recall` skill (`/skill:recall`).

Topic: $@

Use existing artifacts under `<learning-root>/<domain>/<subject>/` if present.
