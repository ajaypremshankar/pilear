---
description: Mock system design exercise
argument-hint: "<design prompt>"
---

Load and follow the `mock-design` skill (`/skill:mock-design`).

Design prompt: $@

Write artifacts under `<learning-root>/<domain>/<subject>/`. Use the active learning root from session context.
